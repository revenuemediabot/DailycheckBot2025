## Структура проекта

- `main.py` — точка входа, инициализация бота.
- `config.py` — вся конфигурация из .env и общие переменные.
- `bot/` — логика запуска и middlewares.
- `handlers/` — команды и колбэки.
- `services/`, `models/`, `database/`, `ui/`, `utils/`, `localization/` — см. описание в исходной структуре.

## TODO
- Описать основные команды
- Пример .env
- Документация по архитектуре
