# ============================================================================
# DailyCheck Bot Dashboard v4.1.0 - ПРОВЕРЕННЫЕ ЗАВИСИМОСТИ
# Только существующие и протестированные пакеты для стабильного деплоя
# ============================================================================

# Core FastAPI Framework (ОБЯЗАТЕЛЬНО)
fastapi==0.104.1
uvicorn[standard]==0.24.0

# Template Engine (ОБЯЗАТЕЛЬНО)
jinja2==3.1.2

# Environment Variables (ОБЯЗАТЕЛЬНО)
python-dotenv==1.0.0

# Data Validation (ОБЯЗАТЕЛЬНО)
pydantic==2.5.0

# Database Support (ОБЯЗАТЕЛЬНО)
sqlalchemy==2.0.23

# Date/Time Utilities (ОБЯЗАТЕЛЬНО)
python-dateutil==2.8.2

# JSON Handling (РЕКОМЕНДУЕТСЯ)
orjson==3.9.10

# HTTP Responses (РЕКОМЕНДУЕТСЯ)
starlette==0.27.0

# Multipart Form Data (РЕКОМЕНДУЕТСЯ)
python-multipart==0.0.6

# Static Files (РЕКОМЕНДУЕТСЯ)
aiofiles==23.2.1

# HTTP Client (РЕКОМЕНДУЕТСЯ)
httpx==0.25.2

# ============================================================================
# ВАЖНО: 
# - sqlite3 = встроенный модуль Python (НЕ УСТАНАВЛИВАЕТСЯ)
# - CORS = встроена в FastAPI (НЕ НУЖЕН python-cors)
# - Все остальные пакеты - опциональные
# ============================================================================

# Опциональные пакеты (можно добавить при необходимости):
# diskcache==5.6.3          # Кэширование на диске
# redis==5.0.1              # Redis поддержка
# psycopg2-binary==2.9.9    # PostgreSQL поддержка
# cryptography==41.0.8      # Дополнительная безопасность
# psutil==5.9.6             # Мониторинг системы
